// const express = require('express');
// const upload = require('../config/fileUpload'); // Multer configuration for handling file uploads
// const { uploadCaseDocuments } = require('../controllers/fileUploadController');
// const authMiddleware = require('../middleware/authMiddleware'); // Ensure user is authenticated

// const router = express.Router();

// // Upload documents related to a case (PDFs, images, etc.)
// router.post('/:caseId/upload', authMiddleware, upload.array('caseDocuments'), uploadCaseDocuments);

// module.exports = router;
